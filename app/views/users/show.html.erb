<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
    <section class="user_info">
      <p>
        <%= "Name: " + @user.real_name %>
      </p>
      <p>
        <%= "Email: " + @user.email %>
      </p>
      <p>
        <%= "Gender: " + @user.gender %>
      </p>
      <p>
        <%= "Birthdate: " + @user.birthdate.to_s %>
      </p>
      <p>
        <%= "Address: 〒" + @user.code %>
      </p>
      <p>
        <%= @user.address %>
      </p>
    </section>
  </aside>
</div>
